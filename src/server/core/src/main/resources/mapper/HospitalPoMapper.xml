<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//dtd/Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhenbanban.core.infrastructure.persistence.mapper.HospitalPoMapper">
    <!-- 业务医院表(hospitals)表映射 -->

    <resultMap id="HospitalPoOptionMap" type="java.util.Map">
        <id property="id" column="id" javaType="Long"/>
        <result property="hospitalName" column="hospital_name" javaType="String"/>
    </resultMap>

    <insert id="insert" parameterType="com.zhenbanban.core.infrastructure.persistence.po.HospitalPo" useGeneratedKeys="false">
        INSERT INTO hospitals (id, ownership_type, hospital_type, hospital_level, insurance_code, uscc_code,
                               hospital_code, hospital_name, hospital_name_pinyin, hospital_name_pinyin_abbr,
                               province_id, province, city_id, city, county_id, county, address, postal_code, longitude, latitude, map_url,
                               contact_phone, contact_email, website, summary, description, companion_diagnosis_enabled,
                               meal_service_enabled, testing_delivery_enabled, created_at)
        VALUES (#{id}, #{ownershipType}, #{hospitalType}, #{hospitalLevel}, #{insuranceCode}, #{usccCode},
                #{hospitalCode}, #{hospitalName}, #{hospitalNamePinyin}, #{hospitalNamePinyinAbbr}, #{provinceId}, #{province},
                #{cityId}, #{city}, #{countyId}, #{county}, #{address}, #{postalCode}, #{longitude}, #{latitude}, #{mapUrl},
                #{contactPhone}, #{contactEmail}, #{website}, #{summary}, #{description}, #{companionDiagnosisEnabled}, #{mealServiceEnabled},
                #{testingDeliveryEnabled}, #{createdAt})
    </insert>

    <update id="update" parameterType="com.zhenbanban.core.infrastructure.persistence.po.HospitalPo">
        UPDATE hospitals
        SET ownership_type              = #{ownershipType},
            hospital_type               = #{hospitalType},
            hospital_level              = #{hospitalLevel},
            status                      = #{status},
            insurance_code              = #{insuranceCode},
            uscc_code                   = #{usccCode},
            hospital_code               = #{hospitalCode},
            hospital_name               = #{hospitalName},
            hospital_name_pinyin        = #{hospitalNamePinyin},
            hospital_name_pinyin_abbr   = #{hospitalNamePinyinAbbr},
            province_id                 = #{provinceId},
            province                    = #{province},
            city_id                     = #{cityId},
            city                        = #{city},
            county_id                   = #{countyId},
            county                      = #{county},
            address                     = #{address},
            postal_code                 = #{postalCode},
            longitude                   = #{longitude},
            latitude                    = #{latitude},
            map_url                     = #{mapUrl},
            contact_phone               = #{contactPhone},
            contact_email               = #{contactEmail},
            website                     = #{website},
            summary                     = #{summary},
            description                 = #{description},
            companion_diagnosis_enabled = #{companionDiagnosisEnabled},
            meal_service_enabled        = #{mealServiceEnabled},
            testing_delivery_enabled    = #{testingDeliveryEnabled},
            updated_at                  = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        UPDATE hospitals
        SET deleted_at = UNIX_TIMESTAMP(NOW(3)) * 1000
        WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="Long" resultType="com.zhenbanban.core.infrastructure.persistence.po.HospitalPo">
        SELECT *
        FROM hospitals
        WHERE id = #{id}
    </select>

    <select id="findIdByCode" resultType="Long">
        SELECT id
        FROM hospitals
        WHERE hospital_code = #{hospitalCode}
    </select>

    <select id="findAll" resultType="com.zhenbanban.core.infrastructure.persistence.po.HospitalPo">
        SELECT *
        FROM hospitals
        WHERE deleted_at = 0
    </select>

    <select id="count" parameterType="java.util.Map" resultType="int">
        SELECT COUNT(id) FROM hospitals
        <where>
            <if test="id != null and id.length > 0">
                AND id = #{id}
            </if>
            <if test="deleted != null and deleted">
                AND deleted_at > 0
            </if>
            <if test="deleted != null and !deleted">
                AND deleted_at = 0
            </if>
            <if test="hospitalCode != null and hospitalCode.length > 0">
                AND hospital_code = #{hospitalCode}
            </if>
            <if test="keywords != null and keywords.size() > 0">
                AND (
                <foreach item="w" collection="keywords" separator="OR">
                    hospital_code LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name_pinyin LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name_pinyin_abbr LIKE CONCAT('%', #{w}, '%')
                </foreach>
                )
            </if>
        </where>
    </select>

    <select id="search" parameterType="java.util.Map" resultType="com.zhenbanban.core.infrastructure.persistence.po.HospitalPo">
        SELECT * FROM hospitals
        <where>
            <if test="id != null and id.length > 0">
                AND id = #{id}
            </if>
            <if test="deleted != null and deleted">
                AND deleted_at > 0
            </if>
            <if test="deleted != null and !deleted">
                AND deleted_at = 0
            </if>
            <if test="hospitalCode != null and hospitalCode.length > 0">
                AND hospital_code = #{hospitalCode}
            </if>
            <if test="keywords != null and keywords.size() > 0">
                AND (
                <foreach item="w" collection="keywords" separator="OR">
                    hospital_code LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name_pinyin LIKE CONCAT('%', #{w}, '%')
                    OR hospital_name_pinyin_abbr LIKE CONCAT('%', #{w}, '%')
                </foreach>
                )
            </if>
        </where>
        ORDER BY
        <choose>
            <when test="orderBy != null and orderBy != ''">
                #{orderBy}
            </when>
            <otherwise>
                id DESC
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
        <if test="offset == null and limit != null">
            LIMIT #{limit}
        </if>
        <if test="offset != null and limit == null">
            LIMIT #{offset}, 18446744073709551615
        </if>
    </select>

    <select id="findNameMapByIds" resultMap="HospitalPoOptionMap">
        SELECT id, region_name
        FROM regions
        WHERE id IN
        <if test="ids != null and !ids.isEmpty()">
            <foreach item="id" collection="ids" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

</mapper>
